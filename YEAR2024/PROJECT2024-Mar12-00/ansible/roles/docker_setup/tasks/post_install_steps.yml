---
- name: Create docker group if it doesn't exist
  group:
    name: docker
    state: present

- name: Add current user to the docker group
  user:
    name: "{{ ansible_user }}"
    groups: docker
    append: yes
  become: yes
